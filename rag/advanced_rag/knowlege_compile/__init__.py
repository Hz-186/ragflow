"""知识离线编译与结构化提取组件包。

包含文档结构编译（structure）、维基知识库编译（wiki / wiki_incremental）、
RAPTOR 递归聚类树（raptor）、思维导图提取（mind_map_extractor）以及数据集全库导航树（dataset_nav）。

各子模块的调用方一律直接导入子模块（如 ``from rag.advanced_rag.knowlege_compile.wiki import ...``），
本包不提供包级再导出。
"""
